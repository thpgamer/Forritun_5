use crate::stada;

pub struct Hestur{
    pub id:u32,
    pub nafn:String,
    pub aldur:i32,    
    pub stada:String,
}
impl Hestur {
    pub fn new(id: u32, nafn : &str, aldur : i32, stada : &str){

    }
}